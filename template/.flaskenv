# 指定主程序实例文件名
FLASK_APP=app

# 设定运行环境 开发环境：development 生产环境(默认)：production
FLASK_ENV=development

# 单独控制调试模式开关(不推荐手动设置)；0(关闭-默认值)，1(开启)
# FLASK_DEBUG=0

# 运行时设定 host 地址；默认值：127.0.0.1
# FLASK_RUN_HOST=127.0.0.1

# 运行时设定 port 端口；默认值：5000
# FLASK_RUN_PORT=5000
